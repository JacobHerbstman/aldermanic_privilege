# census_block_2020_cleaning

Purpose: Handles census block 2020 cleaning.

Produces: Produces files such as `output/census_blocks_2020.csv`, `output/census_blocks_2020_boundary_diagnostics.csv` and related task-specific outputs in `output/`.

Approx. runtime: ~1-10 minutes.

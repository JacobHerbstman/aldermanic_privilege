# calculate_ward_boundary_distances

Purpose: Calculates ward boundary distances.

Produces: Produces files such as `output/boundary_distance_summary.csv`, `output/parcel_geometry_diagnostics.csv`, `output/parcels_pre_scores.csv` and related task-specific outputs in `output/`.

Approx. runtime: ~1-10 minutes.

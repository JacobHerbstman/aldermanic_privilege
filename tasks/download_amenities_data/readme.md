# download_amenities_data

Purpose: Downloads amenities data.

Produces: Produces files such as `output/cta_stops.gpkg`, `output/major_streets.gpkg`, `output/parks.gpkg` and related task-specific outputs in `output/`.

Approx. runtime: ~10-60+ minutes.

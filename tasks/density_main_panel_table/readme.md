# density_main_panel_table

Purpose: Builds density main panel table outputs.

Produces: Produces files such as `output/fe_table_bw250_density_main_panel.tex` and related task-specific outputs in `output/`.

Approx. runtime: ~under 5 minutes.

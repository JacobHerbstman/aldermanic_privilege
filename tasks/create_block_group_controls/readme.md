# create_block_group_controls

Purpose: Creates block group controls.

Produces: Produces files such as `output/block_group_controls.csv`, `output/block_group_geometry_2019.gpkg` and related task-specific outputs in `output/`.

Approx. runtime: ~1-10 minutes.

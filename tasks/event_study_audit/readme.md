# event_study_audit

Purpose: Audits event study.

Produces: Produces files such as `output/rental_audit_summary.txt`, `output/rental_pretrend_tests.csv`, `output/rental_spec_comparison.csv` and related task-specific outputs in `output/`.

Approx. runtime: ~under 5 minutes.

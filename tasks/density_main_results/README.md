# Main density results

This task estimates the paper's binary ward-boundary specification for FAR and
DUPAC and produces the main regression table, sample summary, and residualized
RD figure. All outputs use the frozen project file from
`tasks/new_construction_analysis_data`.

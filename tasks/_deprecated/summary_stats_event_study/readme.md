# summary_stats_event_study

Purpose: Builds summary statistics for event study.

Produces: Produces files such as `output/summary_stats_event_study.csv`, `output/summary_stats_event_study.tex` and related task-specific outputs in `output/`.

Approx. runtime: ~under 5 minutes.

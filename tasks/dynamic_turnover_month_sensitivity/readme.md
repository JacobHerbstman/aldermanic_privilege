# dynamic_turnover_month_sensitivity

Purpose: Handles dynamic turnover month sensitivity.

Produces: Produces files such as `output/boundary_distance_summary.csv`, `output/dynamic_turnover_assignment_by_year.csv`, `output/dynamic_turnover_assignment_summary.csv` and related task-specific outputs in `output/`.

Approx. runtime: ~1-10 minutes.

# create_appendix_a_sumstats

Purpose: Creates appendix a sumstats.

Produces: Produces files such as `output/rental_sumstats_table.tex` and related task-specific outputs in `output/`.

Approx. runtime: ~1-10 minutes.

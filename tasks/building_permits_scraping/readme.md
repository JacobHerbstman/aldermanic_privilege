# building_permits_scraping

Purpose: Scrapes and assembles raw building permit data.

Produces: Produces cleaned scrape outputs and permit geometry files in `output/`.

Approx. runtime: ~10-60+ minutes.

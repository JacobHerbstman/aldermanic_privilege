# slide_deck_audit

Purpose: Audits slide deck.

Produces: Produces files such as `output/geometry_geocoding_checks.csv`, `output/object_verification_index.csv`, `output/rerun_reproducibility_log.csv` and related task-specific outputs in `output/`.

Approx. runtime: ~under 5 minutes.

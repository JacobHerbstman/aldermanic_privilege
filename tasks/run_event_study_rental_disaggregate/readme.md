# run_event_study_rental_disaggregate

Purpose: Runs event study rental disaggregate specifications.

Produces: Produces files such as `output/did_coefficients_rental.csv`, `output/did_table_rental.csv`, `output/did_table_rental.tex` and related task-specific outputs in `output/`.

Approx. runtime: ~3-20 minutes.

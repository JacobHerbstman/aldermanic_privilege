# combine_did_tables

Purpose: Combines DID tables.

Produces: Produces files such as `output/did_table_combined.tex`, `output/did_table_sales_announcement.tex`, `output/did_table_simple.tex` and related task-specific outputs in `output/`.

Approx. runtime: ~under 5 minutes.

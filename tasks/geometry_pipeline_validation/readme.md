# geometry_pipeline_validation

Purpose: Handles geometry pipeline validation.

Produces: Produces files such as `output/canonical_pair_universe.csv`, `output/geometry_validation_report.md`, `output/validation_density_dupac.pdf` and related task-specific outputs in `output/`.

Approx. runtime: ~1-10 minutes.

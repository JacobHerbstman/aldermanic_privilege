# uncertainty_score_density_robustness

Purpose: Runs uncertainty score density robustness specifications.

Produces: Produces files such as `output/alderman_uncertainty_index_baseline.csv`, `output/alderman_uncertainty_index_days_unlogged.csv`, `output/alderman_uncertainty_index_drop_bach.csv` and related task-specific outputs in `output/`.

Approx. runtime: ~1-10 minutes.

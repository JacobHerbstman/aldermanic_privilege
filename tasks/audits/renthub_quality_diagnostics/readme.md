# renthub_quality_diagnostics

Purpose: Retains the broader RentHub source, timing, price, coordinate, and proxy diagnostics outside the production pipeline.

The paper does not depend on this task. Production location corrections are built by `tasks/renthub_location_corrections`.

Produces: `output/chicago_rent_panel_quality_flags.parquet`.

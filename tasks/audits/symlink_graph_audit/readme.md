# symlink_graph_audit

Purpose: Audits the paper Make DAG for missing targets, stale dependency edges, and live upstream data calls.

Produces: `output/make_dag_audit.md` and `output/make_dag_audit.csv`.

Approx. runtime: ~1 minute.

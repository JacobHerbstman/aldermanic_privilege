# city_point_geocode_audit

Purpose: Audits city point geocode.

Produces: Produces files such as `output/coordinate_hotspots.csv`, `output/coordinate_quality_summary.csv`, `output/dataset_inventory.csv` and related task-specific outputs in `output/`.

Approx. runtime: ~under 5 minutes.

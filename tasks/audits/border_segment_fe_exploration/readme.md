# border_segment_fe_exploration

Purpose: Handles border segment FE exploration.

Produces: Produces files such as `output/segment_assignment_coverage_rental.csv`, `output/segment_assignment_coverage_sales.csv`, `output/segment_fe_comparison_summary.md` and related task-specific outputs in `output/`.

Approx. runtime: ~2-15 minutes.

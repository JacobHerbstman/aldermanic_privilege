# manuscript_verification_audit

Purpose: Audits manuscript verification.

Produces: Produces files such as `output/artifact_map.csv`, `output/challenge_results.csv`, `output/claim_artifact_map_hostile.csv` and related task-specific outputs in `output/`.

Approx. runtime: ~under 5 minutes.

# event_study_permit_diagnostics

Purpose: Handles event study permit diagnostics.

Produces: Produces files such as `output/permit_balance_did_shift.csv`, `output/permit_balance_did_shift.tex`, `output/permit_balance_joint_summary.csv` and related task-specific outputs in `output/`.

Approx. runtime: ~1-10 minutes.

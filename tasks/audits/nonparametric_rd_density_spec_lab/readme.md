# nonparametric_rd_density_spec_lab

Purpose: Handles nonparametric RD density spec lab.

Produces: Stages linked upstream inputs in `input/` and writes task-specific outputs to `output/` when run.

Approx. runtime: ~2-15 minutes.

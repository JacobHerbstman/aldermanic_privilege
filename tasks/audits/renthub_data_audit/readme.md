# renthub_data_audit

Purpose: Audits renthub data.

Produces: Produces files such as `output/benchmark_fetch_status.csv`, `output/building_layer_repeat_frequency.csv`, `output/company_building_type_composition.csv` and related task-specific outputs in `output/`.

Approx. runtime: ~under 5 minutes.

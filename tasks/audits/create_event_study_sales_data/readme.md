# create_event_study_sales_data

Purpose: Creates event study sales data.

Produces: Produces files such as `output/sales_block_month_panel.csv`, `output/sales_block_year_panel.csv`, `output/sales_stacked_panel.csv` and related task-specific outputs in `output/`.

Approx. runtime: ~1-10 minutes.

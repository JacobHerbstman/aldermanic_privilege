\section{Density at Ward Boundaries}
\label{sec:empirical_results_density}

\subsection{Border Fixed Effects Regressions}

Table~\ref{tab:density_main_table} is the main density result. In the multifamily sample within 500 feet of a ward boundary, a one standard deviation increase in aldermanic stringency lowers log(FAR) by 0.14 and log(DUPAC) by 0.15. Those estimates imply about 14\% lower FAR and 15\% lower dwelling units per acre on the more stringent side of otherwise similar boundary segments.

I estimate:
%
\begin{equation} \label{eq:segment_fe}
    \ln(Y_{iwst}) = \beta \cdot \text{Stringency}_{w} + \lambda_L D^-_{iwst} + \lambda_S D^+_{iwst} + X_i'\theta + \alpha_{\text{zone group}} + \alpha_{\text{segment}} + \gamma_t + \varepsilon_{iwst}
\end{equation}
%
where $Y_{iwst}$ is a density outcome for parcel $i$ in ward $w$ on boundary segment $s$, built in year $t$; $D^-_{iwst}$ and $D^+_{iwst}$ are side-specific distances to the ward boundary on the lenient and stringent sides; and $X_i$ collects own-side demographic controls. The fixed effects enter additively: zoning-group fixed effects absorb differences across broad zoning environments, segment fixed effects absorb differences across boundary segments, and year fixed effects absorb citywide trends. The main sample uses new multifamily construction within 500 feet of a ward boundary. Standard errors are clustered at the ward-pair level. Appendix Figure~\ref{fig:pruned_border_segments} maps the boundary segments that enter the analysis.

The segment fixed effects are the central design element. Boundary segments are short geographic units, much shorter than full ward-pair borders that can stretch for miles through different neighborhoods, so the comparison stays local. Because each segment lies between exactly two wards, $\beta$ is identified by the cross-border stringency gap within that segment. Segments with little difference in aldermanic stringency contribute little identifying variation; segments with a large gap contribute more. The side-specific distance terms absorb smooth within-side gradients in development density as one moves toward or away from the boundary. Zoning-group fixed effects ensure that parcels are compared within broad zoning environments rather than across fundamentally different parts of the zoning code.

\begin{table}[htbp]
    \caption{Effect of Aldermanic Stringency on Development Density: Multifamily Sample (500 ft)}
    \label{tab:density_main_table}

    \input{../tasks/border_pair_FE_regressions/output/fe_table_bw500_multifamily_zonegroup_segment_year_additive_clust_ward_pair.tex}

    \footnotesize
    \textit{Notes:} Sample includes new multifamily construction within 500 feet of ward boundaries. Regressions include side-specific linear distance-to-boundary controls and own-side controls for share White, share Black, median household income, bachelor's share, and homeownership rate. Fixed effects are listed in the table. Regressions are unweighted. Stringency is the standardized aldermanic stringency score. Standard errors clustered at the ward-pair level. * $p<0.10$, ** $p<0.05$, *** $p<0.01$.
\end{table}

\subsection{Visual Evidence}

Figure~\ref{fig:rd_fe_plots} plots the same 500-foot multifamily sample using a residualized local-linear RD display. The figure is noisier than the segment fixed-effects table, especially for FAR, but both outcomes move in the expected direction at the ward boundary. I read the plot as a visual check that the break occurs at the jurisdictional boundary and lines up with the sign of the regression result.

\begin{figure}[htbp]
    \centering
    \begin{minipage}{0.46\textwidth}
        \centering
        \includegraphics[width=\textwidth]{../tasks/nonparametric_rd_density_linear_display/output/nonparametric_rd_density_linear_display_log_density_far_bw500_multifamily.pdf}
    \end{minipage}
    \hfill
    \begin{minipage}{0.46\textwidth}
        \centering
        \includegraphics[width=\textwidth]{../tasks/nonparametric_rd_density_linear_display/output/nonparametric_rd_density_linear_display_log_density_dupac_bw500_multifamily.pdf}
    \end{minipage}
    \caption{Local-Linear Spatial RD: Multifamily Sample (500 ft)}
    \figurenotes{Left: Log(FAR). Right: Log(DUPAC). Sample includes new multifamily construction within 500 feet of ward boundaries. Outcomes are residualized against zoning-group, boundary-segment, and construction-year fixed effects plus own-side demographic controls before plotting. Colored dots show binned residual means and colored lines show side-specific local-linear fits. Negative distance indicates the more lenient alderman's side; positive distance indicates the more stringent side.}
    \label{fig:rd_fe_plots}
\end{figure}

\subsection{Supporting Checks}

Appendix~\ref{sec:appendix_density} reports the rest of the density evidence. It presents the 250-foot all-construction sample as a benchmark alternative specification, a pruned version of the main multifamily fixed-effects table, the cross-border stringency split, shifted-cutoff placebo plots, donut RD plots, bandwidth-path figures, and a corner-clean table that drops parcels close to other ward-pair boundaries. I treat those exercises as supporting checks rather than as separate main results.

% !TEX root = ../paper.tex

\section{Additional Price Boundary Checks}
\label{sec:appendix_price_rd}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.88\textwidth]{../tasks/price_boundary_results/output/price_boundary_placebos.pdf}
    \caption{Placebo Cutoffs for Listed Rents and Home Sale Prices}
    \figurenotes{Neither rents nor sale prices change significantly across an artificial cutoff.
    The left column places an artificial cutoff 1,000~ft inside the less-stringent ward; the right column places it 1,000~ft inside the more-stringent ward.
    The top row reports listed rents and the bottom row reports home sale prices.
    Each model uses a 500~ft window around the artificial cutoff and otherwise follows Equation~\eqref{eq:price_rd}.
    Each point is the estimated log difference between a 100~ft distance band and the band immediately below the artificial cutoff.
    Subtitles compare the bands immediately above and below the cutoff.
    Shaded areas are 95\% confidence intervals.
    Standard errors are clustered by ward pair.}
    \label{fig:price_placebo_boundaries}
\end{figure}

\begin{figure}[p]
    \centering
    \textbf{Panel A: Locally straight boundaries}\par
    \includegraphics[width=0.80\textwidth]{../tasks/price_boundary_results/output/price_boundary_straight.pdf}

    \vspace{0.6em}
    \textbf{Panel B: Excluding observations nearest the boundary}\par
    \includegraphics[width=0.80\textwidth]{../tasks/price_boundary_results/output/price_boundary_donuts.pdf}
    \caption{Additional Price-Boundary Robustness Checks}
    \figurenotes{Panel A retains locally straight boundaries: a 328~ft line centered on the nearest boundary point stays within 49~ft of the boundary at both ends.
    Panel B excludes observations within 25~ft (left) or 50~ft (right); the top row reports listed rents and the bottom row home sale prices.
    All models otherwise follow Equation~\eqref{eq:price_rd}.
    Points report log differences from the nearest 100~ft band on the less-stringent side, normalized to zero; subtitles compare the bands across the boundary.
    Panel B uses the remaining observations in each band after exclusion.
    Shaded areas are 95\% confidence intervals, and standard errors are clustered by ward pair.
    * $p<0.10$, ** $p<0.05$, *** $p<0.01$.}
    \label{fig:price_boundary_robustness}
    \label{fig:price_straight_boundaries}
    \label{fig:price_boundary_donuts}
\end{figure}
